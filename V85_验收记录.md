# v85 验收记录

日期：2026-09-08。范围：本地 `codex/playability-v85`，不代表线上版本已升级。

## 自动检查

命令：`npm run build:standalone`、`npm test`、`git diff --check`。

最终文件实际大小：4,315,312 字节，约 4.32 MB／4.12 MiB。单文件与 `dist/index.html` 逐字节一致。本机 HTTP 返回 200，与文件长度一致。原打包日志以 JavaScript 字符数冒充字节数，现已修正，因此不沿用日志中偏小的“4.06 MB”口径。

构建 SHA-256：`5f48155867886dc4719a5839510f589ec7c75c5a47405864c23b7d539c9a6015`。

最近一轮 25 项检查通过：

- 单文件脚本可解析，静态 HTML 无重复 ID，包含重新启程确认模块，体积低于 4.5 MiB。
- 结算预览不改变状态；确认后的剩余时间、金额与预览一致。
- 重复结算不重复收费、加时间或发奖励。
- 旧存档停在已完成关卡时，继续到下一关。
- 全程恰好 80 天赢，80.1 天输；此前节省的时间可以弥补末程误车。
- 销毁后的旧回调无效；暂停中的完成回调等待恢复；结果只提交一次。
- 暂停清空长按输入；无效状态与系统键盘自动重复不会产生额外击打。
- 13 个小游戏分别实例化、运行和绘制 120 帧、暂停、恢复、销毁；不改动主线账本。
- 伦敦经度需要向东转满一圈；无障碍控制实验里，不加速不能按时到达，持续加速可以到达。
- 大西洋空格只击打第三轨；战象触屏跳跃不再同时触发冲锋。
- 音游在时钟零点暂停、恢复后，仍从同一拍继续。
- 静音不丢失当前主题；静音期间进入另一关也能选定其主题，恢复声音后不永久失去配乐。
- 红海选择抢修真的恢复准点，选择降速保留半天延误，金额相应变化。
- 11 个实际章节脚本在预设成功／迟归结果下，走完预览、结算、存档与终局分支。

边界：Canvas、DOM、音频在这些逻辑检查中使用替身。120 帧是基础运行检查，不等于十三关真人全流程通关；预设结果串联测试也不等于已测完所有难度、碰撞与叙事分支。

## 浏览器实测

使用本地 HTTP 入口和真实页面交互，没有通过修改浏览器中的游戏状态跳关。

已确认：

- 封面可加载，关2可从自由挑战进入；检查期间浏览器错误日志为空。
- 关2说明保持等待，直到点击开始；开局有倒数。
- 关2的 Esc 暂停弹出菜单；“重新练这一关”回到教程，不弹出清空主线确认。
- 关2运行中打开地图，暂停标记生效；停留超过原本剩余时长后，关闭地图仍回到进行中的同一局（剩余 24 秒），没有在后台结束。
- 自由挑战完整走到低分结算，显示 0 分与独立纪录，主线仍是 80 天／£20,000。
- 新主线可以跳过惠斯特；序章完成后刷新出现继续入口；继续后直接进入多佛，资金不变，没有重复序章。
- 主线进行中，自由挑战按钮均不可启动，并说明如何回封面练习。
- 主线实际误船后选择等候：预览 71.5 天；确认前 HUD 仍 80 天，确认后才变成 71.5 天；退出后继续直接进入关2，余款仍 £20,000。
- 主线剧情阶段也可暂停并返回封面；没有正在进行的操作挑战时，局内重试按钮不可用。
- 844×390 横屏布局：修复前舞台宽约 457 像素、Canvas 宽约 693 像素，右侧被裁；修复后两者一致，舞台完整居中，教程放大并可读。

## 仍需验证／尚未做完

- 真实 iPhone／Android 多指触控、浏览器音频解锁、蓝牙耳机延迟与系统切后台。
- 十三关完整真人游玩、操作手感和不同帧率下的难度公平性。
- 全流程真机二周目、每个重试节点、所有付费补救选择的组合。
- 新的输／赢结局已做逻辑验证，尚未从第一关纯手动打到两种结局各一遍。
- 离线文件在不同浏览器的 `file://` 入口。本轮主要验收本地 HTTP，并未把文件名当作所有环境可双击打开的保证。
- 配乐的主观听感与原创性／素材授权复核；本轮没有新做一首正式配乐。
- 公网部署后回归。本轮未部署、未推送，也未打包原生应用。

## 不应遗忘的产品欠账

当前仍以固定路线串联 13 款小游戏。南海的弹幕玩法、美国连续战斗、大西洋第二套音游与原著事件之间的关系，需要下一轮产品取舍。新增了可靠的输赢，不代表每一关已经值得重玩。

护照里一些早期章节日期仍沿用预设文本；旅程实际总天数以账本和结算为准。后续应把每站日期统一从行程推导，避免提前到达却仍盖固定日期。

旧版可能已有超额花费或自动胜利的历史存档。本轮只修复继续关卡的位置，不反向重写玩家已保存的旧结局、资金或成就。
