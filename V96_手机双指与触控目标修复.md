# v96 · 手机双指与触控目标修复

日期：2026-09-17。状态：本地修复、构建与浏览器验收通过，未部署。

## 本轮找到的问题

1. `InputManager` 只保存一份画布指针状态，却没有记住这份状态属于哪根手指。主手指按住画布时，第二根手指的移动或松开会改写主手指坐标，甚至提前结束拖动。
2. 系统发出 `pointercancel` 时，画布按下状态没有专门清理路径，可能留下卡键。
3. 844×390 横屏下，原方向键折算后约 28px，动作键约 39px，手指命中容错不足。
4. 旧金山组合技只有画布点击区和键盘 Q，没有独立触屏键，而画布点击区与左下方向区部分重叠。
5. 触屏设备初始化输入系统时会立即揭开默认方向键和 A/B 键，尚未选关时就可能覆盖在封面上。

## 最小根因修复

- 记录当前画布 `pointerId`；只允许该手指更新坐标和结束拖动，其他手指不再相互干扰。
- 增加 `pointercancel` 清理，暂停、切关和失焦时仍复用原有 `reset()`。
- 保留虚拟方向键与动作键各自独立的按下/松开，可同时转向和攻击。
- 画布和虚拟按键加入 `touch-action: none`，避免浏览器手势抢走指针事件。
- 手机短横屏将方向键和动作键增至 88 逻辑像素；844×390 下折算约 47.7px。高度不足 350px 时进一步增至 100 逻辑像素。
- 旧金山显示 C 键“组合技”，并将 `justC` 接入原有组合技输入缓冲；桌面 Q 和画布点击继续保留。
- 初始化时保持整层触控键隐藏，只有具体小游戏调用 `configureUI()` 后才显示相应按键。

## 验证证据

- 先让新回归在旧代码上失败：第二根手指会把坐标从 `(100, 200)` 改成 `(700, 400)`。
- 修复后：第二根手指不改写主手指；主手指取消会清理；方向键和 A 键可同时按住并独立松开。
- 旧金山键盘 Q、画布按钮和触屏 C 三条组合技路径都能在受击停顿中进入输入缓冲。
- 新增触屏首屏回归：初始化不得揭开按键层，关卡明确配置后才允许显示。
- 全套 **84 / 84** 自动检查通过，0 失败、0 跳过；`git diff --check` 通过。
- 实际浏览器 844×390 检查：画面保持居中、外层滚动为 0；旧金山简报显示“Q · 触屏 C / 组合技”；运行时方向区和 A/B/C 均配置正确；控制台 0 条 error / warning。
- 单文件与 `dist/index.html` 逐字节一致，均为 **4,293,856 字节（4.09 MiB）**；SHA-256：`2fb1cf5e7520e57e5e9035c788a9fd9bb49463fdf755cdd85ed3c16cbe3aef96`。

## 仍保留的边界

本轮把可以用代码和浏览器确认的触控逻辑问题修好了，但当前电脑浏览器不能代替真机对手指命中率、多指舒适度、震动和 iOS / Android 边缘手势的最终判断。

本机入口：[http://127.0.0.1:5175/](http://127.0.0.1:5175/)。预览服务只监听本机 `127.0.0.1`。
