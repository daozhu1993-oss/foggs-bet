# v89 · 大西洋节拍同步与验收

日期：2026-09-11—12。状态：本地同步机制与判定坐标修复，构建后 **75 / 75 自动检查通过**；Chrome 独立页完成局部操作及零操作失败验收。真实听感、手机触控和后半程连续游玩仍未验收，未部署。

这是 v88 之后的增量记录。沿用 `foggs-bet-v85` 工作副本，不覆盖历史交接或原 Antigravity 工程。

## 修复了什么

1. **音乐和音符不再各走各的时钟。** 大西洋音乐原先由 220 毫秒定时器推进，谱面每拍 0.375 秒，音符又靠逐帧位移抵达。现在音乐、谱面位置、倒计时共用音频时钟，160 BPM，每拍 0.375 秒；连打音符严格放在半拍上。
2. **开场留两小节数拍。** 第一颗音符在第 3 秒抵达；此前显示 1—4 数拍提示。音乐提前排程 120 毫秒，四轨判定盘随同一拍点明暗变化。阅读说明期间仍不计时。
3. **修正金线与判定中心的 60 像素错位。** 控制台平移后，原渲染金线在画布 x=290，实际判定和音符却以 x=230 为中心。现在只换算一次坐标；音符中心、判定盘、金线相同，轨道纵向中心与点击区域也统一。轨道标识改为 1 / 2 / 3 / 4，与玩法卡一致；旧键位别名仍保留。
4. **暂停、静音、卡顿和重试共用正确的拍点进度。** 暂停及退出取消已排程音源；继续时不重播已过去的拍。静音期间只跳过已经过去的拍，取消静音前尚未到达的下一拍不会被提前丢掉。长帧不集中补播旧音乐或多声漏拍反馈。结束后停止本关配乐，重试从零开始。
5. **保持现有规则与存档边界。** 不改 50 秒到港、20 连击全速、到港且最高连击 ≥35 省一天、失败补救及旅费规则。大西洋自由挑战改用 `atlanticBurning_v89` 最高分键；旧无版本分数和 v88 分数保留，其他关卡纪录不变。

按 Ponytail 原则复用原生 Web Audio、现有配乐/输入/结果卡，没有新增引擎、音乐文件或依赖。设备延迟仅保留 `timingOffsetMs` 参数，默认 0、限幅 ±200 毫秒；正值让显示与判定稍晚。尚未对任何真实设备校准，也未新增设置界面。

技术依据：音频时间线可用于精确排程，暂停音频上下文时不继续前进，见 [MDN currentTime](https://developer.mozilla.org/en-US/docs/Web/API/BaseAudioContext/currentTime)。已经调用 start 排程、尚未发声的音源可以提前 stop 取消，见 [MDN stop](https://developer.mozilla.org/en-US/docs/Web/API/AudioScheduledSourceNode/stop)。这些 API 保证不代表扬声器或蓝牙耳机的端到端延迟已经验收。

## 验证证据

### 自动检查

2026-09-12 构建后运行 `npm test`：**75 项通过，0 失败、0 跳过**，比 v88 新增 7 项同步检查。既有前半程、南海、后半程规则与账本检查继续通过。

- 全部音符落在 160 BPM 半拍格点；音频时钟推进、极小帧间隔输入时，音符和倒计时仍处于相同位置。
- 用真实音乐引擎配合音频节点替身，在 30 / 60 / 120 fps 下检查发声时间、预排队列及音源回收，没有独立定时器。
- 暂停于零时刻或拍中、音频时钟冻结或继续推进时，取消未来音源并正确恢复；多次继续不重复排程。
- 静音再开、20 秒长帧、结算、销毁及重试不会重播积压拍点；长帧多次漏拍只发一次反馈声。
- 不支持音频时采用固定的静音时钟；之后出现音频上下文不会把本局时间突然换源；校准参数限幅有效。
- 对真实轨道绘制函数记录平移后的坐标，验证四个判定盘、音符中心、轨道编号、同拍亮度与轨道边界点击映射。
- 正常定时输入控制器仍能在 30 / 60 / 120 fps 完成到港；零操作不能到港；旧分数原样保留。

先复现再修复的失败包括：完整谱面缺少统一拍点、音符仍受帧间隔驱动、取消静音漏掉下一拍，以及渲染目标实际 `{x: 290, y: 468.75}` 不等于判定中心 `{x: 230, y: 467.25}`。上述回归最终全部通过。

音频节点替身检查的是排程调用，不是实际声波或音色质量；坐标记录也不是截图验收。自动控制器通关不代表首次上手难度适合人类。

### 实际 Chrome 页面

此前的锁屏阻挡本轮已解除。浏览器专用连接仍报错，但通过 Chrome 原生界面新开独立标签页，访问 `http://127.0.0.1:5175/`，没有操作用户原有游戏标签页。

1. 封面可见 **v89 大西洋节拍同步版**，自由挑战可直接进入大西洋；说明卡内容与开始按钮正常显示。
2. 实际画面显示四轨 1—4、金色判定盘与音符；顶部仪表未与全局怀表重叠。
3. 一次数字键序列产生 **PERFECT、330 分、最高 1 连击**。这证明页面上确实能击中，不声称每个键都已逐一完成真人验收。
4. Esc 暂停、继续、音效/静音按钮切换、暂停菜单重新练习均能操作；重新练习返回准备卡。
5. 重开后完全不操作，结束显示 **0 分、58% 航程、未到港的实绩说明**；页面仍显示自由挑战不改变主线存档。结果卡“再试一次”可返回准备页。
6. 上述操作后，Chrome 控制台显示 **0 messages**，未见错误或警告。页面留在大西洋准备卡，没有自动开始下一局。

浏览器出现过“正在播放音频”状态，但当前工具没有提供实际声波监听。这不能替代听感验收，也没有证明蓝牙延迟、每次暂停后的主观拍感或整体配器平衡。

## 仍待验收

- 实际听一整局：第一拍、半拍连打、暂停恢复、静音恢复是否都跟手；音效是否盖过音乐。先用扬声器或有线耳机，再比较蓝牙。
- 真机横屏四轨点击、系统来电/切后台后的恢复；本轮桌面点击和逻辑替身不替代手机验证。
- 横滨 → 旧金山 → 列车/雪橇 → 大西洋 → 伦敦连续游玩；本轮仅验收大西洋局部页面和失败路径，没有完成连续真人通关。
- 完成体验验收并获得发布确认后，才合并到原工程及公网。不要把本地 v89 当成线上版本。

## 本地交付

- 工程：`/Users/gx/Codex/00-岛主工作中枢/03-岛主AI内容工厂/foggs-bet-v85`。
- 试玩：[本机 v89](http://127.0.0.1:5175/) → “先试一关 · 自由挑战” → “关9”。服务只监听 127.0.0.1；2026-09-12 HTTP 检查返回 200。
- 单文件 `Fogg赌约_双击直接玩.html` 与 `dist/index.html` 逐字节一致，各 **4,293,096 字节（4.09 MiB）**。
- SHA-256：`daadc1818750382a1aca643f6c5c960a3a240eeda77cab5c32df54a737d57031`。

复现：先运行 `npm run build:standalone`，再运行 `npm test` 与 `git diff --check`。如本地服务未启动，在工程目录运行 `python3 -m http.server 5175 --bind 127.0.0.1 --directory dist`；已有服务时不要重复启动。不要刷新进行中的游戏，新开页或退出关卡后再加载新版。

本轮代码集中在 `src/minigames/atlanticBurning.js`、`src/engine/dynamicAudio.js`、`src/engine/sprites.js`；关联关卡说明、最高分版本键、封面/构建版本和 `tests/playability.test.mjs`。工作副本保留既有未提交修改，不能把全部 Git 差异归为 v89。
