# v95 · 后半程连续验收与手机横屏位移修复

日期：2026-09-17。状态：本地修复、构建与浏览器验收通过，未部署。

## 本轮完成了什么

在同一个浏览器会话中连续操作后半程，不靠单元测试代替页面验收：

- 横滨：完成三幕表演并主动与福克相认，正常成功结算。
- 旧金山：使用移动、出拳、踢击和组合技击倒对手，正常成功结算。
- 列车：实际射击、换弹、专注，验证暂停和“重新练这一关”返回正确简报。
- 雪橇：实际变道、加速、漂移，验证暂停后继续；未赶上时给出正确失败结算。
- 大西洋：四轨节拍输入、暂停和恢复正常；未到港时给出正确失败结算。
- 伦敦：用拖动完成经度时差阶段，进入马车冲刺；未赶上时给出正确失败结算。

整段连续验收未发现跨关状态污染，暂停弹窗的关卡名称均正确，浏览器控制台 0 条 error / warning。

## 手机横屏发现的真问题

在 844×390 横屏视口进入旧金山关卡简报时，聚焦“我准备好了·开始”按钮会触发浏览器的自动滚动：

- `#game-app.scrollTop` 从 0 变为 99.5。
- 整个 16:9 画面向上偏移约 100 像素，顶部被裁，底部出现黑边。
- 根因是缩放后的 1280×720 子容器位于 `overflow: hidden` 父容器中；该父容器仍会成为可被焦点自动滚动的滚动容器。

## 最小根因修复

- 为 `#game-app` 增加 `overflow: clip`，使它不再成为可滚动容器；保留前一行 `overflow: hidden` 作为旧浏览器回退。
- 关卡简报默认聚焦改为 `focus({ preventScroll: true })`，从交互源头禁止聚焦带动外层。
- 新增回归检查，锁定这两条防线。

## 修复后验收证据

- 844×390 下画面等比显示为 693.33×390，左右居中，顶部为 0，无上移。
- 简报按钮保持焦点时，`#game-app.scrollTop = 0`；简报顶部、标题、操作说明和开始按钮均可见。
- 进入旧金山实战、暂停、返回封面后，滚动值仍为 0，控制台 0 条 error / warning。
- 全套 **80 / 80** 自动检查通过，0 失败、0 跳过；`git diff --check` 通过。
- 单文件与 `dist/index.html` 逐字节一致，均为 **4,292,892 字节（4.09 MiB）**；SHA-256：`137de4d6901e079295825f501ad7682e1a25911e206483cbde0bdf0594f594c7`。

## 还有哪些不能冒充已验收

- **桌面后半程功能链路**：本轮已完成连续浏览器操作验收，不再是未验项。但自动操作不能替代真人对节奏、手感和难度的主观判断。
- **手机横屏布局**：本轮模拟验收已通过；但当前浏览器不是真实触屏设备，无法证明虚拟按键的手指命中率、多指同按和震动手感。
- **音乐与环境音**：大西洋四轨输入、暂停/恢复、节拍时钟和结算链路已通过；但音量平衡、听感疲劳和节奏好不好听，必须由玩家实际听。

本机入口：[http://127.0.0.1:5175/](http://127.0.0.1:5175/)。预览服务只监听本机 `127.0.0.1`。
