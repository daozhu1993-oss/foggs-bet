# 《Fogg 的赌约》Codex 阶段增量交接（交还 Antigravity AI）

> 交接日期：2026-08-30  
> 项目根目录：`/Users/gx/.gemini/antigravity/scratch/foggs-bet`  
> 本文性质：对原 `CODEX_HANDOVER.md` 的增量补充，不覆盖原文  
> 当前验收关卡：Leg 2「苏伊士—红海—孟买」

## 0. 接手后的第一条动作

**先请用户体验并确认关卡 2 新版音乐与操作手感，不要直接修改或推进关卡 3。**

用户已经指出旧版关卡 2“缺少一个有节奏感的音乐”。Codex 已补上与谱面同步的原创 120 BPM 蒸汽工业配乐，并重新构建发行文件，但用户在音乐修改后尚未给出最终主观确认。后续 AI 应先收集用户对音量、节奏强度、音色和判定手感的反馈，再决定是否微调。

## 1. 当前事实状态

- 关卡 0、1：本阶段未改动。
- 关卡 2：代码修复、玩法重做、视觉统一、配乐补强和本地构建均已完成。
- 关卡 2 用户验收：**部分完成**。用户已发现旧版音乐缺失；新版音乐已实现，但尚未收到“确认通过”。
- 关卡 3：只做过源码只读审查，**未修改、未完整试玩、未验收**。
- 关卡 4 以后：本阶段未触碰。
- 对外发布/线上部署：未执行。

项目真实主线为 `leg0.js` 至 `leg10.js`，共 11 段。`londonFinale.js` 由 `leg10.js` 调用；项目中没有 `leg11.js`。原交接文档中把最终伦敦段写成 Leg 11，是编号描述偏差，后续以源码为准。

## 2. 关卡 2 已完成内容

### 2.1 主线与结算

- 修正 `resolveLeg` 调用参数与结果卡数据结构。
- 基础航程按 11 天计算，结算印章只写入孟买章。
- 结果卡先展示预览，只有用户选择“继续”时才调用 `resolveLeg` 写入状态。
- 选择“重试”不会重复扣钱、增加天数、重复盖章或累加完美通关次数。
- 结算规则保留：基础司炉奖金 `-£500`；S 级减免 `£300`；B 级可选择追加维修费或接受延误。
- 通关后正确进入 `leg3`。

### 2.2 节奏玩法

- 原先的轮船资源管理原型已改成经典双轨节奏音游。
- 曲速：120 BPM，4/4 拍。
- 单局时长：45 秒。
- 谱面：131 个固定音符；不使用随机谱面，重试时节奏一致。
- 判定窗口：`PERFECT ±70ms`、`GREAT ±160ms`。
- 操作：
  - 红轨：`D / A / ←`
  - 蓝轨：`K / L / →`
  - 金色双音：`空格 / J / 两键同时按`
- 修复 `KeyD` 同时被输入系统映射为右方向，导致 D 键误触蓝轨的问题。
- 曲目时间改用 WebAudio 单调时钟；暂停、恢复会补偿时间差，避免谱面跳拍。

### 2.3 新增原创配乐

- 根因：原代码调用 `playTheme('suez')`，但 `DynamicMusicEngine` 中没有 `suez` 分支，所以实际只有很轻的节拍提示，没有完整音乐。
- 现在由音游时钟每个八分音符调用 `playSteamStep(stepIndex)`，配乐与谱面共用同一个时间源。
- 配乐结构：低音蒸汽脉冲、底鼓、军鼓、金属闭镲和黄铜短旋律。
- 两小节基础循环包含 8 个低音脉冲、4 个底鼓、10 个金属打击和 4 个旋律音。
- 暂停时游戏停止触发音乐步进；恢复后从正确节拍继续。
- 全部由原生 WebAudio 实时合成，无外部音频文件、无版权音乐、无新增运行时依赖。

### 2.4 视觉与游乐场

- 去掉与风暴原画重复的程序化轮船绘制，保留并裁切现有红海风暴原画。
- 下半区统一为维多利亚轮机舱的黄铜、深铁、旧木色调，减少现代荧光感。
- 音符改为黄铜包边的八角机械键帽样式。
- 顶部航程状态条下移，避免与全局 HUD 重叠。
- 更新关卡 2 游乐场卡片、玩法文案和操作简报。
- 修复战术简报被标题页盖住的问题（简报层级提高到 `z-index: 110`）。
- 从游乐场启动关卡 2 时，HUD 地点显示“红海 · 蒙古号轮机舱”；结束后恢复原地点与对话框状态。

## 3. 本阶段修改文件

| 文件 | 修改说明 |
|---|---|
| `src/levels/leg2.js` | 关卡 2 主线、结算预览、重试事务边界、孟买印章 |
| `src/minigames/steamOverdrive.js` | 固定谱面、WebAudio 时钟、输入修复、判定、画面与配乐同步 |
| `src/engine/dynamicAudio.js` | 新增 `playSteamStep(stepIndex)` 原创蒸汽配乐 |
| `src/main.js` | 关卡 2 战术简报、游乐场 HUD 地点和对话框恢复 |
| `index.html` | 关卡 2 游乐场卡片文案与入口按钮 |
| `style.css` | 战术简报层级修复 |
| `dist/index.html` | 重新生成的 41.9 MB 单文件发行版 |
| `Fogg赌约_双击直接玩.html` | 重新生成的 41.9 MB 单文件发行版 |

不要把本阶段临时安装的依赖或测试文件复制进项目。目标项目当前没有新增 `node_modules`、测试框架或音频素材。

## 4. 已完成的验证

### 4.1 静态与逻辑验证

- `node --check src/engine/dynamicAudio.js`：通过。
- `node --check src/minigames/steamOverdrive.js`：通过。
- 固定谱面验证：两次生成均为 131 个音符，内容一致。
- 配乐结构小检查：16 个八分音符步进得到 8 个低音、4 个底鼓、4 个旋律音与 10 个噪声打击。
- 关卡 2 一次性流程桩测试通过：重试前状态不变、确认继续后只结算一次、返回 `leg3`。

### 4.2 构建与浏览器验证

- `npm run build`：通过，Vite 共转换 57 个模块。
- 构建时的“大于 500 kB”警告来自 `images.js` 内嵌约 43 MB Base64 原画，是当前架构的已知警告，不是本次构建失败。
- `node build_standalone.js`：通过；生成约 41.90 MB 单文件。
- 本地 `http://127.0.0.1:5173/`：标题页、游乐场、关卡 2 简报和实际开局均能进入，浏览器控制台无错误或警告。
- 桌面横屏与 390×844 竖屏保护层此前做过本地检查；竖屏会提示旋转设备。

### 4.3 当前发行物校验值（SHA-256）

```text
85c336a965ffedf5e08990371b350a64593d1173aec0994687b01f5a7cb93dde  src/engine/dynamicAudio.js
ab1632c18b354e86dca7a3ccf772ded4b8d9a586d23a171c7318dc43840ebda9  src/minigames/steamOverdrive.js
0f12c3a81b1de68ef31345d6c0a61cadbeb0f4934aaec7c05f0966060efded9d  dist/index.html
0f12c3a81b1de68ef31345d6c0a61cadbeb0f4934aaec7c05f0966060efded9d  Fogg赌约_双击直接玩.html
```

## 5. 推荐启动与构建方式

### 5.1 开发试玩

```bash
cd /Users/gx/.gemini/antigravity/scratch/foggs-bet
npm install
npm run dev -- --host 127.0.0.1
```

浏览器打开：`http://127.0.0.1:5173/`

进入路径：`探险游乐场` → `关2 · 蒙古号红海风暴超频` → `进入轮机舱`。

说明：Codex 内置浏览器会拦截 `file://` 本地页面，因此应使用 localhost。外部浏览器可尝试直接打开 `Fogg赌约_双击直接玩.html`，但开发验收仍推荐 localhost。

### 5.2 交付构建

```bash
cd /Users/gx/.gemini/antigravity/scratch/foggs-bet
npm run build
node build_standalone.js
```

`build_standalone.js` 会直接读取源码、`index.html` 和 `style.css`，并同时覆盖：

- `Fogg赌约_双击直接玩.html`
- `dist/index.html`

任何源码修改后都必须重新运行该脚本，不要只改开发态源码。

## 6. 尚未完成与明确边界

1. **关卡 2 新配乐尚未获得用户最终体验确认。** 不要把“代码已实现”写成“用户已验收”。
2. 用户可能继续提出：音乐太轻、太吵、不够燃，或者判定声盖过配乐。优先只调整 `playSteamStep()` 中的音量、音色与节奏密度，不要换技术栈。
3. 本阶段没有新增真实录音、MP3、外部曲库或第三方音频依赖。
4. 本阶段没有部署网页，也没有核验任何公开 URL。
5. 关卡 3 及后续关卡不得在关卡 2 未确认前继续开发。

## 7. 关卡 3 已知风险（只读发现，尚未修复）

对应文件：

- `src/levels/leg3.js`
- `src/minigames/elephantRide.js`
- `src/minigames/stealthRescue.js`

`leg3.js` 当前在显示结果卡之前就调用：

```js
const record = resolveLeg('leg3', resultData);
const action = await resultCard.show(record);
```

这意味着用户选择“重试”时，可能再次扣除 `£2,000`、再次增加天数、重复写入印章与完成记录。修复方式应复用 `leg2.js` 的事务模式：

1. 先根据两段小游戏结果构造纯 `resultData`。
2. 用当前状态计算结果卡预览，不修改全局状态。
3. 只有 `action === 'next'` 时才调用一次 `resolveLeg('leg3', resultData)`。

除此之外，战象突进和神庙营救的实际难度、输入、画面统一性和移动端表现均未完整验收，不能预设为完成。

## 8. 原 AI 接手检查清单

- [ ] 阅读原 `CODEX_HANDOVER.md` 与本增量交接。
- [ ] 启动 localhost，确认音效按钮处于开启状态。
- [ ] 让用户先体验关卡 2 新配乐。
- [ ] 根据用户反馈只做关卡 2 的小范围调音或手感修正。
- [ ] 重新运行语法检查、Vite 构建和单文件构建。
- [ ] 用户明确确认关卡 2 后，再开始关卡 3。
- [ ] 关卡 3 首先修复“重试重复结算”，再验收 3A 与 3B。
- [ ] 每完成一关都重新生成单文件，并让用户体验确认后再进入下一关。

---

本交接只记录已经核验的事实。没有经过用户体验、浏览器实测或线上验证的内容，均明确标为未完成。
