# v91 · 雪橇碰撞修复与横滨复验

日期：2026-09-15。状态：雪橇道具接触面错位已做最小根因修复；横滨 v90 修复后页面验收已补齐。构建后 **77 / 77 自动检查通过**，本地浏览器无错误或警告，未部署。

## 本轮实际发现

在本地独立试玩页进入关 8b，雪橇、道具与赛道都能显示，玩法说明、失败结果和重试入口正常。但实际画面显示，道具会先从雪橇下方穿过，稍后才进入碰撞判定。

回归探针确认了错位：同一条横向轨迹上，障碍物临近判定点时绘制在 `(934.08, 717.90)`，雪橇实际位于 `(830, 560)`。根因是两者使用了不同的近景投影：道具以画布底部 `y=720`、横向半宽 `590` 为接触面，玩家却固定在 `y=560`、横向半宽 `380`。

## 修复

按 Ponytail 原则，只修改 `src/minigames/iceSledge.js` 中道具的两条透视换算：

- 道具近景纵向终点从画布底部对齐到雪橇接触线 `y=560`。
- 道具近景横向半宽从 `590` 对齐到玩家移动半宽 `380`。

赛道形状、速度、转向、漂移、碰撞阈值、道具类型、计分、25 秒时限、结算和存档均未改变。版本标识更新为 **v91 雪橇碰撞修复版**。

## 验证

- 新增“雪橇道具应在碰撞面与玩家相遇”回归；修复前按上述坐标失败，修复后通过。
- 雪橇专项 **5 / 5** 通过，覆盖零操作失败、正常控制在 30/60/120 fps 到站、道具接触面和超车只计一次。
- 全套 **77 / 77** 通过，0 失败、0 跳过；`git diff --check` 通过。
- 单文件重新构建；`Fogg赌约_双击直接玩.html` 与 `dist/index.html` 逐字节一致，均为 **4,292,372 字节（4.09 MiB）**，SHA-256：`8bf713a9855b95621923630d98520ad2e88457ca25c5070497a7a2f910d55f91`。
- 浏览器确认 v91 版本页；修复后雪橇道具保持在车辆前方并朝统一接触线靠近，不再画到车辆下方才判定。
- 浏览器零操作雪橇只完成 94%，正确显示未到奥马哈与“再试一次”，重试回到玩法说明且自由挑战不改主线。
- 页面控制台 **0 条 error / warning**。

## 横滨补验

v90 的修复后页面已补齐以下证据：

- 第二幕重心条与绿区始终可见，没有 toast 遮挡。
- 绿区按空格后实际显示 `PERFECT POSE`，姿势从 `0 / 2` 增至 `1 / 2`。
- 第三幕主动飞扑可触发重聚，结果卡正确写明接住 2 件道具、0 次姿势但与福克和艾娥达重聚；“再试一次”回到说明页。
- 另一局未主动相认会显示错过重聚，不制造成功结果。

## 仍待玩家验收

自动控制已证明雪橇可在 30/60/120 fps 以正常输入到站，但当前浏览器控制只能发送短按，不能替代玩家真实长按空格、连续漂移的手感判断。因此仍保留三项边界：

1. 玩家完成一次桌面雪橇连续通关，确认转向与碰撞的主观手感。
2. 手机横屏触控验收。
3. 后半程连续游玩与大西洋真实听感验收。

若不出现新问题，桌面连续验收与小修约 30–60 分钟；手机、声音和后半程连贯体验再约 60–90 分钟。该估计不包含用户提出新的玩法重做或公网发布。

本机入口：[http://127.0.0.1:5175/](http://127.0.0.1:5175/)。当前可直接试玩的页面已保留在应用内浏览器，服务只监听本机 127.0.0.1。
