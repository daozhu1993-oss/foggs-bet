# v90 · 横滨提示修复与验收

日期：2026-09-12。状态：修复已在实际页面发现的提示遮挡；构建后 **76 / 76 自动检查通过**。修复后页面复验被再次锁屏阻挡，雪橇页面验收尚未完成，未部署。

## 发现与修复

在 Chrome 独立试玩页进入横滨，实际截图显示：第二幕的转场提示和重复姿势提示叠在重心条上，玩家看不见用于判断姿势时机的绿区。根因是本关仪表从 y=86 开始，而全局 toast 位于 y=85；第二幕同时出现两条提示，之后还会反复弹出。

按 Ponytail 原则，复用已有幕次仪表、玩法卡和底部操作区，删掉本关重复弹窗及不再需要的提示定时字段；不移动全局 toast，也不改已认可的前半程。

- 桌面第二幕操作区常驻“绿区内按空格 · 摆姿势”，触屏 A 标为“绿区 · 摆姿势”。
- 第三幕继续保留点福克/空格指引；开始飞扑后改为进行中的说明，不再继续提示重复起跳。
- 演出时长、接道具/姿势计分、主动相认条件、旅费、补救和最高分键保持不变。

## 验证

新增回归先在修复前失败：进入第二幕后收集到 3 条覆盖式提示，预期为 0。修复后验证幕次仍正常推进、提示留在操作区、绿区说明和飞扑入口未丢失。

构建后全套 **76 项通过，0 失败、0 跳过**；原有长按防重复、最后时刻飞扑、暂停结束动画、30/60/120 fps 正常输入通关和账本检查继续通过。单文件解析、重复 ID、体积上限与 `git diff --check` 通过。

本轮修复前实际页面已观察到第二幕、重心条遮挡，以及主动飞扑中暂停/恢复。尚未观察到该局最终结果卡；不可记为已完成浏览器通关。v89 的大西洋页面证据仍见对应记录，不外推为本轮横滨或雪橇验收。

电脑在后续截图时再次锁屏，已请求用户手动解锁；没有绕过锁屏或用模拟渲染冒充页面复验。雪橇本轮只读了玩法、控制与渲染代码，没有新的页面通过结论或代码改动。

## 继续工作的入口

1. 解锁后，在独立页面加载 v90 → 自由挑战 → 关6；确认第二幕重心条始终可见、姿势与相认入口清晰，并走完一次结果/重试。
2. 再进入关8b，检查转向/加速/漂移提示、道具位置与接取时机、失败结果与重试。25 秒时限是否适合首次上手仍待体验。
3. 真机触控、大西洋真实听感、后半程连续通关仍待验收；没有对原工程或公网发布。

本机入口：[http://127.0.0.1:5175/](http://127.0.0.1:5175/)。服务只监听 127.0.0.1；若停止，在工程目录运行 `python3 -m http.server 5175 --bind 127.0.0.1 --directory dist`。不要强制刷新用户正在进行的游戏。

工程：`/Users/gx/Codex/00-岛主工作中枢/03-岛主AI内容工厂/foggs-bet-v85`。单文件 `Fogg赌约_双击直接玩.html` 与 `dist/index.html` 逐字节一致，各 **4,292,370 字节（4.09 MiB）**；SHA-256：`93f8d4abb6bacfab1373d01e604a8677b638b12c87e1094f5dc6f639ee993808`。

本轮源码改动仅横滨提示、对应回归与封面/构建版本；保留之前的 v89 大西洋同步及其他未提交修复。复现检查：`npm run build:standalone`、`npm test`、`git diff --check`。
